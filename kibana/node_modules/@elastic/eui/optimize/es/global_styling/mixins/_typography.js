/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0 and the Server Side Public License, v 1; you may not use this file except
 * in compliance with, at your election, the Elastic License 2.0 or the Server
 * Side Public License, v 1.
 */
import { euiLineHeightFromBaseline, euiFontSizeFromScale } from '../functions/typography';
import { useEuiTheme } from '../../services/theme/hooks';

/**
 * Returns font-size and line-height
 */
export var euiFontSize = function euiFontSize(scale, euiTheme) {
  var measurement = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 'rem';
  return {
    fontSize: euiFontSizeFromScale(scale, euiTheme, measurement),
    lineHeight: euiLineHeightFromBaseline(scale, euiTheme, measurement)
  };
}; // Hook version

export var useEuiFontSize = function useEuiFontSize() {
  var scale = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 'm';
  var measurement = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 'rem';

  var _useEuiTheme = useEuiTheme(),
      euiTheme = _useEuiTheme.euiTheme;

  return euiFontSize(scale, euiTheme, measurement);
};